import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <img
          src={icon}
          alt="web-development"
          className="w-16 h-16 object-contain"
        />

        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        As a passionate Software Engineer from Ranchi, Jharkhand, India, I
        specialize in creating dynamic and user-friendly digital solutions. With
        extensive experience in Full Stack Development, I'm skilled in Java,
        JavaScript, Node.js, Python, React.js, and more. My expertise in data
        structures, Object-Oriented Programming, Low Level Design and High Level
        Design combined with my proficiency in a wide range of tools like
        SpringBoot, Hibernate, AWS, and various databases, allows me to build
        robust and scalable applications. My work includes notable projects like
        an E-Commerce platform, influencer tools for Shopify, and various
        serverless applications on AWS. I bring a unique blend of innovative
        skills, a fast-learning ability, and a knack for adapting new
        technologies to meet user needs. Let's collaborate to transform your
        innovative ideas into reality with efficient and impactful digital
        solutions.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
